# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'testcustom' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for testcustom
   pod 'Alamofire', '~> 4.7'
   pod 'SDWebImage', '~> 4.0'
   pod 'SwiftyJSON', '~> 4.0'
end
